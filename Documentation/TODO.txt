priority high

//sunfd
27. 发布时需要更新导出模板文件

41. 配置好自动录入的环境

33. CServer::GetInstance()->GetSale().Update(checkedRows, m_cacheRow).then(new CUpdateListener(*this, m_cacheRow));
如果server接到m_cacheRow中某个字段为空 则不更新这些订单的这个字段 只更新不为空的字段。即Sale修改时 只修改变更部分 没有变更的 不修改

35. plan 同上问题
		CServer::GetInstance()->GetPlan().Update(checkedRows, m_cacheRow)
			.then(new OnPlanUpdateListener(m_cacheRow, *this));
如果某字段为空 则不进行修改

37. 计划更新时，生产日期和包装日期两个审核有一个已经是Y了 那就不能修改 其他字段可以修改

36. Plan计划时，判断出厂生产编号是否重复应该判断除了该订单之外的是否使用 自己使用不应该弹提示

39. 需要在JQGrid里增加setCheck和cleanCheck两个功能 控制某行是否被选中

38. 审核反审核流程再确认下，目前Git上的数据库，进入待办事项后，生产业务和包装计划对应的个数对，但进入审核后出现的条数多了

42. 销售订单界面添加新合同时，左右置选项是“是”和“否”，涵义不清楚,选项应该为“左置”和“右置”

43. 销售订单界面添加新合同时，制动器型号选择后，在添加合同后不显示

//wx
29. 确认崩溃问题是否是由本地配置文件导致的

31. 数据有效性验证

40. 计划反审核时，点击第三个，反审核的是第二个，用GIt上的数据库即可再现

44. 在计划界面选择左上角筛选功能，其中选择“生产日期未计划”时，软件崩溃. 换客户的数据库试验下


QA：

1. 铭牌资料是下拉还是输入

2. 最新Bug里面：目前在计划排产界面，点击“计划”，其中只能修改3个日期和2个编号，在实际工作的流程中，在排产的同时，需要根据轴承的库存，修改起供应商。
这个和需求冲突

3. 投产编号如何自动生成

4.最新0125版bug里，销售页面已经通过2次审核，进入排产的订单，在计划反审核后，订单仍然处于可排产状态，不太明白具体需要回到什么状态

priority low
9. 支持自更新和安装包
2. 键盘的支持
10.支持修改密码和重置、找回密码功能


Done
30. Sale页面默认下拉位置为未审核
26. 登陆页面自动记住上次登陆的用户名
23.导出的数据，表头的优先级位置不应放在最后
8. 导入历史数据
19.选择用户后，自动补全信息: 通过客户名称+规格型号来唯一标识
1. 导出信息到某一特定模板的excel中，需要实现
4. 增加几个字段，参考需求表格
13.导出的excel需要有一个默认的文件名
14.下拉列表输入的文字自动保存在服务器上，目前需要重启，需要修改
3. 导出的内容需要和查询条件配合，不是导出全部，而是导出查询结果
11.表格显示数据首先按订单日期排序 再按优先级排序
12.关键字搜索时，如果搜索的是下拉列表部分，模糊搜索
6. Plan页面对审核状态细化，支持对包装日期、发货日期审核状态的查询
7. 导入用户列表
5. Plan页面支持对生产日期、包装日期和发货日期的查询
15.将优先级的位置放到客户区域后面，不要放到最后
16.sale的add接口增加新的8个节点后不好用
17.sale的modify接口增加新的8个节点后不好用
20.add还是不好用
21.添加超过一页，无法自动刷新到第二页
22.左右置目前显示的是Y和N，需要显示是和否
18.返回的数据扎线长度和铭牌资料顺序反了
